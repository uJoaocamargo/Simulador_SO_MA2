<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <!-- Excluir testes que estão no diretório SimuladorSO.Tests para evitar referência a xUnit no projeto principal -->
  <ItemGroup>
    <Compile Remove="SimuladorSO.Tests\**\*.cs" />
  </ItemGroup>

</Project>
